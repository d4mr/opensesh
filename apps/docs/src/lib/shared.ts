export const appName = "opensesh docs";
// Canonical origin — OG/twitter meta needs absolute URLs for social scrapers.
export const siteUrl = "https://docs.opensesh.io";
// Docs are mounted at the domain root — docs.opensesh.io/guides/…, /api/…
// — matching the URL structure of the previous docs sites.
export const docsRoute = "/";
export const docsImageRoute = "/og";
export const docsContentRoute = "/llms.mdx";
