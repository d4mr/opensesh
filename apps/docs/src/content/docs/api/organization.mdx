---
sidebar:
  order: 1
title: "Organization"
---

1 operation. All requests need an `Authorization: Bearer osk_...` header — see the [API overview](/api) for authentication and conventions.

## Get the organization
`GET /organization`
The organization the API key belongs to, with its members and pending invitations.
```bash
curl https://app.opensesh.io/api/v1/organization \
  -H "Authorization: Bearer osk_..."
```